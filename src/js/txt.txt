html
    <div className="sidebar-expansion-panel">
      <div className="sidebar-expansion-panel__heading">
        <button
          className={cn([
            'sidebar-expansion-panel__expand-btn',
            {
              'sidebar-expansion-panel__expand-btn--rotate': !isExpanded,
            },
          ])}
          type="button"
        ></button>
      </div>
      <div
        className={cn([
          'sidebar-expansion-panel__content',
          {
            'sidebar-expansion-panel__content--expanded': isExpanded,
          },
        ])}
      >
        {/* твои кнопки  */}
      </div>
    </div>

// css
.sidebar-expansion-panel {
  padding: to-rem(24) 0;
}

.sidebar-expansion-panel__heading {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.sidebar-expansion-panel__expand-btn {
  display: inline-flex;
  padding: to-rem(8);
  border: none;
  background-color: transparent;
  color: var(--secondary-color);
  outline: transparent;
  transform: rotate(0);
  transition: transform var(--slide-transition);

  &--rotate {
    transform: rotate(180deg);
  }
}

.sidebar-expansion-panel__content {
  max-height: 0;
  padding-top: 0;
  overflow-y: hidden;
  transition: max-height var(--slide-transition),
    padding-top var(--slide-transition);

  &--expanded {
    max-height: <ВЫСОТА КОНТЕНТА КНОПОК>;
    padding-top: to-rem(24);
  }
}

// variables
  --fast-transition-duration: 0.3s;
  --medium-transition-duration: 0.4s;
  --hover-transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  --slide-transition-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
  --fade-transition-timing-function: cubic-bezier(0.25, 0.8, 0.25, 1);
  --hover-transition: var(--fast-transition-duration)
    var(--hover-transition-timing-function);
  --slide-transition: var(--fast-transition-duration)
    var(--slide-transition-timing-function);